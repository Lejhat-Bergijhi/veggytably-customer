name: veggytably_customer
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=2.19.2 <3.0.0"

dependencies:
  dio: ^5.0.2
  flutter:
    sdk: flutter
  flutter_secure_storage: ^8.0.0
  get: ^4.6.5

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/Subtract.png
    - assets/images/Group 335.png
